---
type: subsystem
summary: Hooks into vanilla menus and windows: settings-menu DEBUG/SOUND injection, build-menu unlock wiring for all 8 categories, button/window dispatch, tooltip/blueprint text.
entry_points:
  - GNX/gml/gml_GlobalScript_s_button_activate.gml
  - GNX/gml/gml_GlobalScript_s_button_text.gml
covers:
  - GNX/gml/gml_GlobalScript_s_button_activate.gml
  - GNX/gml/gml_GlobalScript_s_button_text.gml
  - GNX/gml/gml_GlobalScript_s_window_alpha.gml
  - GNX/gml/gml_GlobalScript_s_slot_windows.gml
last_commit: 03ff8ced27b460b4c06cbb314338491c63e4390c
metadata: {"settings_injection":"3 identical sites in s_button_activate.gml (settings open, guide BACK, tool BACK) — draw override + button shift + entry button","build_menu":"scr_gnx_apply_unlocks / scr_gnx_migrate_unlocks push h_types to unlock.breed/utility/pleasure + b_*/t_* order arrays; b_other uses b_pleasure_order","custom_sprite":"spr_gnx_option_window imported by install_foundation.csx","interact_types":"9001-9010 tool menu, 9020 sound page, 9030/hijacked-45 travel"}
edges:
  - to: gnx-registries type: depends_on reason: build-menu unlock wiring reads cell_registry category fields
---

- Settings-window button placement (verified from goblin_nest.gmx sprite defs): `spr_button_oval` is 7x6 with origin (3,3) — near-centered; `spr_button_arrow` is 6x7 origin (3,3). `scr_draw_hover_button` draws `draw_sprite_ext(spr, img, x, y, ...)` with NO offset, so the button instance's (x,y) IS the sprite centre. Established pattern for a single oval button on a settings text row (BACK, the SFX-page opener button 16): `scr_create_button` y-arg = (that row's `_y + N` offset) + 4 — e.g. BACK label `_y+100` -> button y-arg 112 wait that's +12... CORRECTION: text is drawn fa_top so its visual centre is ~text_top+5; BACK label `_y+100` (top at window.y+108) -> oval y-arg 112 ≈ centre. Net rule: oval y-arg ≈ (8 + N) + 4 where N is the `_y+N` label offset. x: slider rows put left arrow at x-arg 11, right arrow at x-arg 34, value text fa_center at `_x + _width` (= window.x + 23). The goblin-ambience GOBLIN row (2026-09-09) ended at label `_y+74`, value = short label ("OFF"/"BAL"/"3-LN"/"IND"), cycle button `spr_button_oval` at x-arg 34 y-arg 86 (matches the > arrow column + row rhythm). <!-- id:2f60 -->
- Options/settings page (scr_gnx_draw_alpha_window + vanilla scr_create_alpha_window buttons): the right-side button column is x-arg 35 (the > arrow column) — UI PLACEMENT toggle (btn 12, y 85), FULLSCREEN toggle (btn 13, y 98), EXIT oval (btn 15, y 112 then +13 shift), and now the GNX DEBUG entry (interact 9010) which was at x-arg 25 and is now 35 (fixed 2026-09-09, 4 identical `scr_create_button(...9010...)` sites in s_button_activate). GUIDE icon (btn 14) stays at x-arg -5 (left, by its label). GNX inserts a DEBUG text row at `_y+100` and pushes GUIDE/EXIT text from `_y+100` to `_y+113`; correspondingly `with(obj_button) if (button_num==14||15) y += 13` at each injection site. Row rhythm is 13px (text) / 13px (buttons). <!-- id:ddff -->
- GNX option window border fix (2026-09-09): the custom spr_gnx_option_window (169x150) sits with the same xorig=126 as vanilla spr_option_window so the frame's right edge is at window.x + 42, but its right border reads thicker, so buttons at the vanilla x-arg 35 (the > arrow column) visually overlap it. Fix: new helper `scr_gnx_alpha_fixup(_aw)` in s_initials.gml replaces the old inline `with(obj_button) y+=13` at all 4 injection sites — it does the GUIDE/EXIT (btn 14/15) y+=13 AND pulls the right-column ovals (btn 12 UI PLACEMENT, 13 FULLSCREEN, 15 EXIT) x -= 5. DEBUG entry button (9010) created directly at x-arg 30 (was 35→25→35→30 across iterations). EXIT label moved `_x + 43` -> `_x + 34` to clear the shifted oval. Vanilla scr_create_alpha_window positions in s_window_alpha.gml left at x-arg 35 (correct for the vanilla no-tools window). NOT touched: the value-row < value > arrow layout (`scr_create_button_layout(12,11,...,23,...)` -> > at x-arg 35) — if it also clips the GNX frame the fixup would need to shift btn_num 0-11 too and the value text (`_width`=55) left in tandem. <!-- id:0407 -->
- Title-screen GNX version stamp (2026-09-09): `global.gnx_version` = "1.3.14" set at the very top of `scr_create_initials()` (s_initials.gml line ~4, a plain global NOT on global.val so it's not save data; bump on release). New patched file `gml/gml_Object_obj_menu_Draw_0.gml` (vanilla 1.38 obj_menu Draw event + one addition): after the stock `draw_text(479, 263, "v." + string(1.38))` bottom-right version, adds `if (variable_global_exists("gnx_version")) draw_text(479, 253, "GNX " + string(global.gnx_version) + " enabled");` — right-aligned, 10px above the game version (no room below y263 on the 480x270 view). scr_create_initials runs from obj_p Create before the menu draws; the variable_global_exists guard covers the first-frame edge case. This is a NEW file in the patched set — needs deploying to the G3M profile and a diffs/ regen for the public record. <!-- id:3d37 -->
